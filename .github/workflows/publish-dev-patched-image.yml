name: Publish dev patched image

on:
  workflow_dispatch:
  push:
    branches:
      - dev-patched

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/archon

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev-patched

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Set short SHA
        id: vars
        run: echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Build image
        run: |
          docker build \
            -t "$IMAGE_NAME:dev-patched" \
            -t "$IMAGE_NAME:dev-patched-${{ steps.vars.outputs.short_sha }}" \
            .

      - name: Push image
        run: |
          docker push "$IMAGE_NAME:dev-patched"
          docker push "$IMAGE_NAME:dev-patched-${{ steps.vars.outputs.short_sha }}"